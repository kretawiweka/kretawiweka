// import PropTypes from 'prop-types';
import React from 'react';
import './style.scss';

const Header = () => {
  return (
    <div className="navbar">
      <h1 className="nav-brand">{'{this.title}'}</h1>
    </div>
  );
};

export default Header;
